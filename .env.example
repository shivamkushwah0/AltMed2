# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/altmed

# AI Service
GEMINI_API_KEY=your_gemini_api_key_here

# Session Security (generate with: openssl rand -base64 32)
SESSION_SECRET=your_secure_random_string_32_chars_plus

# Development Settings
NODE_ENV=development
PORT=5000

# Optional: Replit specific (only needed for Replit deployments)
# REPL_ID=your_repl_id
# REPLIT_DOMAINS=your-repl-domain.replit.app
# ISSUER_URL=https://replit.com/oidc